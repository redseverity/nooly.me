import { MenuOpen } from "./MenuOpen";
import { MenuClose } from "./MenuClose";
import { MenuRoot } from "./MenuRoot";

export const Menu = {
  Root: MenuRoot,
  Open: MenuOpen,
  Close: MenuClose,
};
